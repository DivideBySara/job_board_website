<h1>DataBright Open Jobs</h1>

<% @jobs.each do |job| %>

  <h3><%= job.title %></h3>
  <p>Last updated: <%= job.updated_at.getlocal.strftime("%B %d, %Y") %></p> <%# t.timestamps creates 2 fields: created_at and updated_at %>
  <h5><%= link_to "Show Posting", job_path(job)%></h5>

<% end %>
